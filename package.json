{
	"name": "aiki",
	"version": "0.1.12",
	"private": true,
	"type": "module",
	"workspaces": [
		"lib",
		"types",
		"server",
		"sdk/workflow",
		"sdk/task",
		"sdk/client",
		"sdk/worker"
	],
	"scripts": {
		"check": "bunx @biomejs/biome check .",
		"check:fix": "bunx @biomejs/biome check --write .",
		"check:types": "tsc --noEmit",
		"test": "bun test",
		"test:watch": "bun test --watch",
		"test:coverage": "bun test --coverage",
		"server": "bun run server/server.ts",
		"server:watch": "bun --watch run server/server.ts",
		"changeset": "changeset",
		"bump": "changeset version && bun run check:fix",
		"clean": "rm -rf lib/dist types/dist server/dist sdk/*/dist",
		"build:lib": "bun run --cwd lib build",
		"build:types": "bun run --cwd types build",
		"build:server": "bun build server/server.ts --compile --outfile server/dist/aiki-server",
		"build:sdk": "bun run --cwd sdk/workflow build && bun run --cwd sdk/task build && bun run --cwd sdk/client build && bun run --cwd sdk/worker build",
		"docker:build": "docker compose build",
		"docker:up": "docker compose up -d",
		"docker:down": "docker compose down",
		"docker:logs": "docker compose logs -f",
		"prepublishOnly": "bun run build:lib && bun run build:types && bun run build:sdk",
		"publish": "changeset publish && git push --follow-tags",
		"prepare": "husky"
	},
	"lint-staged": {
		"*.{ts,tsx,js,jsx,json}": [
			"bunx @biomejs/biome check --write"
		]
	},
	"devDependencies": {
		"@biomejs/biome": "^2.3.10",
		"@changesets/cli": "^2.27.1",
		"@types/node": "^22.10.2",
		"bun-types": "^1.1.38",
		"husky": "^9.1.7",
		"lint-staged": "^16.2.7",
		"tsup": "^8.5.1",
		"typescript": "^5.7.2"
	}
}
